info:
  name: Obter Alertas
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/alertas"
  auth:
    type: bearer
    token: "{{jwt_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
