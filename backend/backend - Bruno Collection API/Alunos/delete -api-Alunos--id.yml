info:
  name: delete /api/Alunos/:id
  type: http
  seq: 5
  tags:
    - Alunos

http:
  method: DELETE
  url: "{{baseUrl}}/api/Alunos/:id"
  params:
    - name: id
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{jwt_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/Alunos/:id"
      method: DELETE
      params:
        - name: id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
